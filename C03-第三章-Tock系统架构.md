# Tock系统架构

Tock是业内首个采用非C/C++编程语言开发的嵌入式操作系统。这一特性使其在操作系统架构与嵌入式安全领域都具有独特价值。本章将深入剖析Tock的构建机制，并阐述其作为安全嵌入式应用开发平台的核心优势。

## 为什么是Tock？

Tock是一款源自学术研究项目的嵌入式操作系统。尽管它可能尚未达到生产就绪状态，但具备一项显著优势：这是首个采用C语言以外的编程语言开发的完整嵌入式操作系统。更值得关注的是，业界也对其应用表现出浓厚兴趣（搜索OpenSK和OpenTitan项目）。它或许不会成为未来广泛使用的操作系统。但毋庸置疑，它必将为其他现代语言编写的操作系统开辟道路。

本书作者坚信，对嵌入式操作系统进行全新视角的审视至关重要。当前所有商用操作系统均基于五十年前确立的相同原理构建。这些系统虽稳健可靠，却难以适应下一代设备的需求。

通过本书，我们试图为读者提供全新的视角。

## 一些有关操作系统的词语

在深入探讨Tock的设计之前，我们必须先了解操作系统的构建方式。为此，下一节将重点阐述内核、驱动程序和应用程序的含义。

### 内核

操作系统本身被称为内核。这是与应用程序共同运行的软件，并负责管理系统所有硬件和软件资源。内核负责决定哪些程序必须运行、哪些程序需要等待、哪些程序被允许访问文件或网络连接等资源。系统中绝大多数任务都由内核执行，例如从文件读取和向文件写入数据、与网络发送和接收数据、读取键盘输入等。

从开发者的角度来看，内核负责与硬件交互，并为在其上运行的程序提供两个标准接口：应用二进制接口（ABI）和应用程序接口（API）。这极大简化了开发者的工作。若没有内核，开发者就必须编写大量代码才能与硬件交互。每种硬件都各不相同，需要采用不同的接口方式。

| 注意 | ABI代表进程向内核发送请求（系统调用）并接收内核返回值所采用的标准。另一方面，API代表进程可向内核发送的请求集合。|
| ---- | ----- |

举个例子，想想声卡。根据制造商的不同，每块声卡暴露的接口不一，这意味着需要使用不同的函数来控制它。当使用内核时，内核会识别声卡类型并知道如何与之交互 —— 换言之，知道调用哪些函数。随后它将声卡API导出给开发者。只要是声卡，开发者就能使用相同的内核API，无需关注具体硬件。只要系统运行的内核导出了相同的声音ABI，同一程序就能在任何系统上运行。

只要系统正常工作，内核对开发者和用户而言完全不可见。用户仅在系统彻底崩溃时才会注意到内核，例如Windows的蓝屏死机或Linux/macOS的系统崩溃（Panic）。

### 驱动程序

既然我们已经明确了内核的功能，接下来就会产生这样的疑问：内核如何知道如何与所有可能的硬件进行交互？答案很简单：它并不知道。内核有一套标准设备接口，支持特定类型的设备，例如硬盘驱动程序器、声卡、显卡、网络设备等。根据实际使用的内核，这些所谓的设备接口存在很大差异。

对于这些接口，内核允许开发者加载插件，也称为驱动程序。这些驱动程序能够与硬件进行交互，并将信息报告给内核。只要内核为特定硬件提供了驱动程序，开发者和程序就能使用该硬件。若未加载驱动程序，内核将无法与该硬件交互，也无法将其功能提供给开发者和程序。

可将内核视为一个庞大的框架，它通过标准接口将驱动程序与程序连接起来：面向驱动程序的驱动程序接口，以及面向开发者的应用程序接口（API）。

### 应用程序

在系统上实际运行并与用户交互的程序称为应用程序。它们通过内核的ABI和API与硬件交互，通常具有可移植性。这意味着只要使用相同的操作系统（内核），应用程序就应该能够运行，无论硬件平台如何。

虽然有些应用程序随操作系统一起发布（但并非操作系统实际组成部分），但大多数应用程序是由用户自行安装的。

### 服务

服务是一种通常在后台运行且不与用户交互的程序。此类程序的例子包括Web服务器或备份系统。它们与多个应用程序交互，并为这些应用程序提供服务。

## Tock系统架构

Tock是一款源于学术环境的嵌入式操作系统（OS）。它最初是一个研究项目，旨在实现一种能够充分利用现代微控制器（MCU）特性的新型操作系统，例如更大的闪存和RAM容量，以及用户模式和内存保护单元（MPU）的存在。现有多数操作系统（如FreeRTOS和Zephyr）虽支持这些新特性，但开发者需投入大量精力通过插件实现。而Tock开箱即用，完整提供所有这些功能。

Tock的另一项独特亮点在于内核与应用程序的分离。对于标准（计算机）操作系统而言，这种分离似乎很常见 —— 你拥有像Windows、Linux或macOS等操作系统，其上运行着多个单独安装的应用程序；但对于嵌入式操作系统而言，情况不同。对于MCU，普遍做法是将操作系统打包成单一二进制文件，其中包含所有应用程序和服务。构建应用程序时，实际上是在构建整个操作系统。

对Tock而言，内核独立构建后上传至设备。随后每个应用程序单独构建并分别上传至开发板。此流程模拟了常规操作系统的行为模式，使应用程序与服务的更新管理更为便捷。

| 注意 | Tock真正脱颖而出之处在于它完全用Rust语言编写，没有一行C语言代码。这堪称重大变革，过去50年来，C/C++始终是操作系统编写的唯一语言。尽管C语言功能强大且稳定，但它已显陈旧，难以适应现代需求。内存管理完全依赖程序员自行掌控，导致编译器无法检测内存错误。请理解，在C语言诞生的年代，确实别无选择。|
| ----- | ----- | 

图3-1展示了完整的Tock架构堆栈。底部红色部分为硬件设备——微控制器（MCU）。它由处理单元（CPU）及若干外设组成，这些外设不仅扩展了功能（如随机数生成器RNG、AES加密），还使其能够与其他硬件交互（GPIO、USB、I2C、ADC、SPI、UART）。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-1.jpg></img></div>
<div align=center>图 3-1 Tock栈</div>

在硬件之上，橙色部分是直接与硬件交互的底层驱动程序，它们向内核和胶囊暴露硬件接口层（HIL）。实际内核（青绿色）主要位于底层驱动程序与胶囊之间，提供进程调度器、进程间通信（IPC）驱动程序及内存管理功能。内核上层（蓝色区域）为胶囊组件。这些上层驱动程序通过HIL相互提供服务，并向用户空间暴露API接口。应用程序（浅绿色）运行于内核之上，并非与内核直接链接，而是通过API调用向内核请求服务。

### 用户空间

在Tock架构的顶层，我们拥有应用程序与服务。这些程序运行于Tock之上，每个程序均可采用任意编程语言编写（只要能被设备MCU架构编译），并像典型计算机应用程序那样独立构建。

应用程序和服务以受限模式运行，这意味着它们仅能访问操作系统授权的资源，无法直接与硬件交互。它们只能使用ABI定义的操作系统系统调用接口。进程同样只能访问内核提供的内存区域，不得访问内核或其他应用程序的内存。

由于运行存在诸多限制，他们运行在用户模式。因此，所有这些应用程序和服务有时统称为操作系统的用户空间。

| 注意 | Tock竭力限制应用程序访问资源。这需要某些硬件支持，特别是MPU。Tock假定其运行的所有MCU均具备此硬件能力。|
| ----- | ----- | 

虽然Tock可以在某些不具备内存保护功能的MCU上运行，例如某些Cortex-M0设备，但在没有MPU的情况下，它无法限制应用程序对内存的访问。

### Tock内核

正如我们之前提到的，内核运行在硬件之上，并负责

• 调度应用程序和服务 —— 决定每个应用程序或服务何时可使用CPU进行处理。
• 内存管理 —— 为应用程序和服务分配内存，并限制未经授权的访问。
• 进程间通信 —— 在应用程序和服务之间传递消息。
• 提供硬件访问接口 —— 为驱动程序（非应用程序和服务）提供称为硬件接口层（HIL）的接口。
• 提供应用程序与服务接口 —— 通过称为系统调用接口（syscall）的机制访问驱动程序暴露的功能。

可以看见，Tock内核不知道如何访问硬件，而是暴露硬件接口层（HIL）。底层驱动程序（图3-1中橙色部分）为上层驱动程序（图3-1中蓝色部分，称为胶囊）提供硬件接口。正如我们稍后将详细说明的，Tock内核并未实际实现这些服务，它仅为胶囊与驱动程序之间提供了一个通用通信接口。

由于内核需要完全访问设备和内存，其代码运行时不受任何内存或访问限制。从技术上讲，它可能执行任何操作，这可能构成安全隐患。由于采用Rust语言编写，内核必须遵守Rust编译器的内存安全规则。这意味着内核代码在编译时就会接受内存错误的检查与验证。相比C语言编写的代码，安全性显著提升。

但存在一个限制。由于内核需要执行特定的系统管理任务，它时不时的需要直接访问内存。在Rust中，可通过unsafe代码块实现此操作。该代码块内，Rust编译器不执行任何检查。Tock团队虽努力将内核中的unsafe代码块数量降至最低，但这类代码块确实存在且无法完全消除。某些任务在没有此特性支持下根本无法完成。

### 硬件驱动程序

如图3-1中橙色部分所描绘的，硬件驱动程序是直接与硬件组件交互的小型插件。它们实现硬件接口层（HIL），向内核暴露功能，并与另一端的硬件接口直接通信。

对于内核而言，硬件接口需要特定的内存访问操作，在Rust中这对应为unsafe代码块。为减少unsafe代码块数量，内核提供寄存器接口供驱动程序使用。这使得所有驱动程序都能采用或多或少相同的方法和代码直接访问硬件。从安全角度看，寄存器接口中的漏洞影响所有驱动程序，同样修复也能同时应用于所有驱动程序。

| 告警 | 尽管Tock内核暴露了寄存器接口，驱动程序仍需要实现额外功能，这需要unsafe代码块。这使得驱动程序的安全性与C代码大致相当。不过，驱动程序一旦编写完成，通常很少改变，且其代码通常经过众多开发者的测试。|
| ----- | ----- | 

### 胶囊

上层驱动程序（称为胶囊）负责向用户空间运行的应用程序和服务提供实际的ABI。它们通过HIL接口调用硬件驱动程序及其他胶囊，并提供系统调用。为使概念更清晰，胶囊分为两类：

• 系统调用胶囊 —— 为用户空间的应用程序和服务提供系统调用。这类胶囊通常为应用程序提供标准API，并将请求转发至与实际设备对接的服务胶囊。
• 服务胶囊 —— 为其他胶囊（两种类型胶囊，系统调用和服务）提供服务。这类胶囊通常了解如何通过标准总线（如SPI、I2C、串行总线等）对接特定硬件设备。

图3-2展示了胶囊连接的示例。在用户空间中，我们有一个应用程序需要获取运动传感器（加速度、磁方位和陀螺仪）的数据。该应用程序通过系统调用调用**NineDoF**（九自由度）胶囊。该胶囊并不知道设备实际配备了哪些传感器，但它通过**NineDoF HIL**从**Fxos8700**胶囊获取服务。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-2.jpg></img></div>
<div align=center>图 3-2 运动传感器的胶囊互连</div>

另一方面，Fxos800胶囊能够与板载的Fxos8700传感器交互。该传感器通过I2C总线通信，因此Fxos8700胶囊借助I2C HIL接口，使用I2C Mux胶囊实现与传感器的数据收发。

### 安全事实

随着当今大多数设备都连接到网络或互联网，安全性正成为一个日益重要的议题。在计算机发展的早期阶段，安全问题只会导致部分计算机无法运行以及某些文档丢失；而如今，安全问题对现实世界的影响要大得多。

在安全方面，Tock采用了若干有意义的方案。部分采用的安全策略快速预览如表3-1所示。Tock遵循的的基本准则如下：

• 无外部依赖 —— Tock内核的所有代码均位于其仓库内部，除Rust核心库外不存在任何外部依赖。这意味着所有内核代码均向希望使用Tock的开发者和供应商开放。
• 胶囊内无unsafe代码 —— 尽管Tock内核需要使用unsafe代码块来直接访问某些内存区域，但胶囊是不被允许这样做的。Rust编译器会拒绝编译包含unsafe代码块的胶囊。这确保胶囊具备内存安全性，且仅能通过HIL访问硬件。
• 应用程序与服务运行于用户模式 —— 这意味着它们无法直接访问硬件，仅能操作自身内存且不能导致系统崩溃。但这需要硬件支持。用户空间安全是更宏大的议题，本书后续将深入探讨。

表 3-1 Tock每个组件的安全事实表

|  组件  | 安全机制 | 需要硬件支持 |
| -------- | -------- | -------- |
| 用户空间应用程序与服务 | 内存保护单元<br>用户模式执行 | 是 |
| 内核 | Rust编译器（带unsafe）（代码必须完全可信） | 否 |
| 硬件驱动程序 | Rust编译器（带unsafe）（代码必须完全可信） | 否 |
| 胶囊 | Rust编译器（若使用unsafe， 不会编译）| 否 |

尽管内核仍使用某些unsafe代码块，但随着Tock开发者不断探索规避或优化这些代码块的方法，其内部代码量已随时间推移逐渐减少。与C语言编写的内核不同，Rust编译器能为大部分源代码提供内存安全与一致性的保障。这无疑是巨大优势，不妨将其视为在构建内核前进行校对的计算机。

## Tock用户空间架构

迄今为止，我们仅讨论了Tock内核内部的运作机制。虽然内核的职责是管理资源并保障应用程序安全，但设备的核心逻辑大多位于运行于内核之上的应用程序中。如前所述，应用程序在受限环境中运行，该环境受到内核的严格管控。它们无法直接访问硬件，必须通过内核API，即实际驱动程序来执行各项任务。

| 注意 | 在深入探讨之前，我们需要先定义“进程”这一术语。进程是指在Tock系统上运行的程序的单一实例。这与应用程序略有不同。应用程序是设备所执行功能的统称。它可能由一个或多个具有共同目标并协同执行的进程组成。|
| ----- | ----- | 

Tock内核通过*AppId*结构体识别每个进程。这是一个唯一且不透明的结构，并按此特性处理。切勿对其进行任何预设假设。

### 进程状态

内核为每个进程存储其状态。表3-2描述了所有可能的进程状态。虽然存在多种状态可用，但核心状态是运行状态和让出状态。这两种状态是应用程序开发人员可见的状态。

表 3-2 进程状态

| 状态 | 定义 |
|------|------|
| 运行(Running) | 进程已准备执行。这并不一定意味着该进程是当前正在微控制器（MCU）上执行代码的进程；它只是随时可以运行。|
| 让出(Yielded) | 进程正在等待某个事件，尚未准备执行。这可能发生在以下两种情况之一：<br>1.该进程已调用*yield*系统调用<br>2.该进程已执行完毕，且无需再次被调度。在这种情况下，该进程永远不会期望有任何回调函数被调度。|
| 停止运行(StoppedRunning) | 进程在运行过程中被停止。这意味着该进程将不会再次被调度，直到手动恢复。如果需要再次调度该进程，必须将其恢复为“运行(Running)”状态。<br>通常，内核绝不会停止进程。这必须由驱动程序来完成。*ProcessConsole*提供了一个用于停止进程的功能。|
| 停止让出(StoppedYielded) | 进程在处于让出状态时被停止。这意味着该进程将不会再次被调度，直到手动恢复。如果需要再次调度该进程，必须将其恢复为“让出(Yielded)”状态。通常下，内核不会停止进程。这必须由驱动程序来完成。*ProcessConsole*提供了一个用于停止进程的功能。|
| 故障(Faulted) | 进程引发了一个错误，可能是内存访问错误，并被内核终止。根据*FaultResponse*，内核可能会<br>1.系统崩溃，打印错误信息，并停止系统<br>2.尝试重启该进程若干次<br>3.终止该进程。|
| 终止(Terminated) | 进程已经执行*exit-terminate*系统调用。|
| 未启动(Unstarted) | 进程还未被启动。|

Tock进程是单线程的，这意味着它们每次只能运行一个代码实例。进程的执行可能会被内核或中断打断，但执行会从中断时的同一位置继续。进程可以调度在发生某些事件时应被调用的函数，但这些函数仅在进程主动请求时才会被调用。

| 注意 | Tock中的进程状态与经典进程状态略有不同。在Linux等经典操作系统中，“运行”状态被称为“就绪”（Ready），而“让出”状态则被称为“等待”或“阻塞”（Blocked）。Tock中没有与Linux中的“运行”进程状态相对应的状态。这是因为Tock是一个单线程操作系统，内核操作无法与任何进程并行执行。表3-3展示了这些状态的对应关系。|
| ----- | ----- | 

表 3-3 Tock与Linux中的进程状态对应

| Tock进程状态 | Linux进程状态 |
|------|------|
| 运行(Running) | 准备与运行(Ready and Running) |
| 让出(Yielded) | 阻塞(Blocked) |
| 停止运行(StoppedRunning) | 终止(Terminated) (当进程运行中) |
| 停止让出(StoppedYielded) | 终止(Terminated) (当进程阻塞中) |
| 故障(Faulted) | 终止(Terminated) (故障) |
| 终止(Terminated) | 终止(Terminated) |
| 未启动(Unstarted) | 新建(New) |

### 系统调用

Tock进程使用系统调用向内核请求服务，并通过预定的的回电(upcall)接收返回信息。内核提供了七个系统调用：*memop*、*readwriteallow*、*readonlyallow*、*subscribe*、*command*、*yield*和*exit*。表3-4对每项进行了详细说明。所有这些系统调用均为异步的，这意味着它们会立即返回一个值，通常表示成功或失败。如果内核需要一些时间来处理请求且无法立即返回响应，则进程需要订阅并等待内核发出的回电。这些回调是预定的的，且仅在进程调用*yield*之后才会发生。按照经典操作系统术语，所有的Tock系统调用均为异步的。

进程仅在以下三种特定情况下才会被中断：
1. 时间片已到期，这意味着该进程的执行时间已超过内核允许的范围。
2. 中断到达；内核必须处理该中断。
3. 进程发生故障，内核将其置于“已停止”状态。

表 3-4 Tock系统调用

| ID | 系统调用 | 描述 |
| ----- | ----- | ----- |
| 0 | Yield | 将进程置于“Yielded”状态，并等待任何预定的的回电被执行。<br>回电返回后，进程将恢复运行，就像它是从*yield*调用中返回一样。|
| 1 | Subscribe | 允许进程注册一个函数，该函数应在请求操作完成后被调用。<br>此函数仅在进程处于“Yielded”状态时才会被执行，这意味着该进程此前已调用过*yield*系统调用，并且正在等待预定的的回电。否则，该回调函数将被排入队列，等待后续的*yield*调用。|
| 2 | Command | 向驱动程序请求操作。*Command*系统调用会返回成功或失败，并返回最多三个数值返回值.<br>返回值通常表示已收到操作请求，驱动程序将尝试执行该操作。|
| 3 | ReadWriteAllow | 允许进程与驱动程序共享一个用于读写的缓冲区。这通常用于从驱动程序内返回大量数据。它与*subscribe*和*command*系统调用配合使用。缓冲区的填充通常不会开始，直到*command*系统调用发出。进程内读取结果的过程发生在预定的的回调函数被调用之后。|
| 4 | ReadOnlyAllow | 允许进程与驱动程序共享一个用于读的缓冲区。这通常用于向驱动程序发送大量数据。<br>它与*command*系统调用配合使用，驱动程序会在执行请求操作时读取数据。|
| 5 | Memop | 允许进程向内核请求更多内存，并向内核报告其内存布局。<br>它用于在进程启动阶段，向内核请求堆内存，并向内核报告，实际栈大小和数据大小，以便进行调试。|
| 6 | Exit | 请求内核终止进程。根据系统调用的参数，内核将<br>0 – 终止该进程<br>1 – 重启该进程。|

除了中断进程外，如果进程发出系统调用，内核还可以重新获得对MCU的控制权。这意味着进程是以自愿的方式将控制权移交给内核的。

| 注意 | Linux拥有几百个系统调用，而Tock只有七个。Linux对每项任务都有一个专门的系统调用，这意味着开发人员通常只需调用一个系统调用即可完成某项任务。对于Tock，开发人员则需要组合五个系统调用才能达到相同的效果。这与CISC与RISC处理器架构的对比非常相似，Tock相当于RISC架构。|
| ----- | ----- |

由于Tock仅提供七个系统调用，因此必须将它们组合使用，才能实现与标准操作系统相同的效果。在使用系统调用时，主要有两种情况：内核可以立即返回结果，也称为同步操作，或者它需要启动一项需要一定时间的操作，并在稍后通知我们。后者称为异步操作。

| 提示 | 这种异步系统调用架构与Node.js的工作原理非常相似。|
| ----- | ----- |

#### 同步系统调用

同步方法通常用于确定某些作为常量的系统参数。例如，通过发出*command*系统调用，进程可以确定系统已连接了多少个GPIO或LED。从内核的角度来看，这只是返回一个常量数值的问题。它并不依赖于外设来获取响应。每当内核必须依赖某些外设或设备来传输数据时，它就无法立即响应。GPIO是一个例外，它可以通过*command*读取。

对串行端口（控制台）、SPI等系统总线进行读写操作，或读取传感器数据（通过总线甚至ADC）等操作，必须以异步方式完成。这种方式效率更高，它允许MCU在外设能够提供所需数据之前先处理其他任务，而不是忙于等待外设完成操作。

| 提示 | 这种异步处理模式已被Nginx Web服务器和Node.js语言等高性能软件所采用。|
| ----- | ----- |

若要向内核请求同步操作，进程必须使用*command*系统调用。在内核将系统调用发送给指定驱动程序期间，进程将被挂起；若驱动程序完成命令操作后，该进程的时间片仍有剩余，则进程将立即恢复。驱动程序处理命令请求所花费的时间将计入该进程的时间片中。

驱动程序可以通过*command*系统调用返回最多三个无符号32位整数，或者最多两个无符号64位整数和一个无符号32位整数。任何其他超出这些限制的响应都必须使用*allowed*的缓冲区进行报告。由于大多数使用缓冲区的系统调用都是异步的，因此我们将在接下来的“异步系统调用”一节中描述这种使用模式。

#### 异步系统调用

绝大多数系统调用都采用异步模式。在这种情况下，进程向内核发出请求。内核会确认该请求，并在任务处理完毕后通过回电（upcall）通知进程。这定义了系统调用的使用模式，也是一个重要的话题。那么，让我们来详细探讨一下。

首先，我们以环境传感器为例。我们假设该传感器通过数据总线连接到我们的MCU，并且能够读取环境温度和湿度。我们还假设这是一个新的传感器，Tock用户空间库中尚未提供对其的支持。进程必须直接向传感器发送系统调用。

直觉上，我们可能会认为进程应向传感器驱动程序发出*command*系统调用，而驱动程序应能立即返回数据。然而，这种假设是错误的。传感器是通过某种数据总线连接到MCU的。读写该总线需要时间，而内核和其他进程可以利用这段时间执行其他有用的操作。

我们的进程不能仅仅发出一个简单的*command*系统调用，而是必须遵循一种模式。虽然数据的实际读取是由*command*系统调用触发的，但我们的进程需要做好准备，以便在命令系统调用返回后的某个后续时间点接收响应。为此，进程需要采取的第一步是订阅(*subscribe*)回电。这意味着使用*subscribe*系统调用，该调用提供了一个函数指针，当传感器数据可用时，该函数将被调用。如果系统调用成功，驱动程序便知道，一旦数据可用，它必须调度调用提供的函数。

| 注意 | “调度”一词的用法非常重要。驱动程序永远无法在进程内部直接调用某个函数。驱动程序所能做的，只是请求内核在某个时间点调用该函数。这就是为什么驱动程序要对调度函数调用。|
| ----- | ----- |

| 提示 | 每个进程都有一个预定的任务队列。默认情况下，该数量限制为10个，但可以在kernel/src/process.rs文件中进行修改。在撰写本文时，Tock支持两种类型的任务:回电（upcalls）和IPC调用。|
| ----- | ----- |

一旦进程成功订阅了传感器驱动程序的上行调用，下一步就是向驱动程序发送读取命令。这是通过调用 command 系统调用实现的，这也是进程实际请求驱动程序执行读取操作并返回结果的时刻。系统调用返回成功应被解释为驱动程序已确认请求并执行了读取操作。这并不意味着驱动程序读取数据一定会成功。驱动程序仅报告其将尽力获取所请求的数据，且就其当时所知，不应存在错误。此时进程可以继续执行其他任务，当数据准备就绪时，驱动程序会通知进程。

现在到了棘手的部分。驱动程序将在后台执行其任务，而其他所有进程仍在运行。一旦它完成数据读取，就会向该进程调度一个回电（前提是该进程确实已订阅了该事件）。进程不会无缘无故地收到通知，而是必须主动请求回电。这里我们需要深入探讨细节。Tock回电是以同步方式执行的。这意味着进程绝不会被突然中断以触发回调函数。要接收回电，进程需要让出执行权。这通过调用*yield*系统调用实现，该调用将停止进程并将其置于“已让出”状态。当进程处于已让出状态时，内核会检查其任务队列中是否有任何待处理（预定）的回电。如果有，内核将把进程置于“运行”状态，并调用队列中第一个预定的回电。随后，为该回电注册的回调函数返回，进程将像*yield*系统调用已返回一样继续执行。换而言之，当回调函数返回时，*yield*系统调用也随之返回。

| 告警 | 如果你具备一些Linux背景知识，可能会认为回电类似于Linux信号，而回调函数类似于信号处理程序。但事实并非如此。Linux信号是异步函数，可以在任何时候被调用。Linux内核会在任意给定点暂停进程并调用信号处理程序。进程并不知道它是在哪里被暂停的，这也是为什么进程在信号处理程序内部能采取的操作是有限的。而 Tock的回电绝不会以这种方式被调用。它们只能在调用*yield*系统调用后被调用。如果进程从未让出，它就永远不会收到回电。这使得回调函数具有确定性，因为进程确切地知道自己是在何处暂停来接收回电。|
| ----- | ----- |

如果一个进程的任务队列中有多个待处理的回电，它需要多次让出控制权才能处理完所有回电。一般规则是：让出一次控制权对应接收一个回电。一旦回调函数被调用，该进程不会自动取消订阅。驱动程序将保留对回调函数的指针，直到该进程订阅一个NULL指针替代函数为止。这才是取消进程订阅的操作。

进程在使用*yield*系统调用时必须非常谨慎。在大多数情况下，进程在让出控制权时，可能只有一个可能的预定的回电。然而，也可能存在这样的情况：当进程让出控制权时，会有多个回电被调度（多个驱动程序正在代表该进程执行某些操作）。当*yield*系统调用返回时，进程必须验证其等待的回调函数是否已被调用。若未被调用，则需要再次让出。

换言之，如果一个进程需要多次发出同一条命令，只需订阅一次，随后发出*command-yield*系统调用对即可。图3-3展示了我们在此讨论的系统调用模式。

| 告警 | 需要特别指出的是GPIO端口。读写微控制器自身的GPIO端口速度非常快，尽管可以异步进行，但这样操作所需的时间反而会比同步读写更长。这就是为什么访问GPIO端口及与GPIO相关的驱动程序（如LED）通常采用同步方式。<br>另一方面，如果GPIO位于通过I2C或SPI等总线连接到微控制器的扩展器中，则必须采用异步读写方式，因为总线数据传输会耗费大量时间。|
| ----- | ----- |

#### 共享缓冲区

观察图 3-3，您可能会发现系统调用模式中多了一个可选的额外步骤：*allow*系统调用。已订阅的回调函数有三个无符号32位（或64位，取决于MCU的字长）整数作为参数。这意味着使用回电报告的结果必须能容纳在三个数字中。这对我们的传感器来说已经足够，因为它很可能只报告两个数值，但对于其他一些驱动程序来说可能不够用，例如网络接口驱动程序。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-3.jpg></img></div>
<div align=center>图 3-3 Tock系统调用模式</div>

为了获取更复杂的数据，进程可以与驱动程序共享缓冲区。与Linux或类似操作系统中的标准驱动程序不同，Tock驱动程序必须遵守所有Rust编译器规则，且不允许使用unsafe关键字。这意味着，与标准驱动程序不同，Tock驱动程序无法访问进程的内存（尽管驱动程序运行时拥有全部内存权限）。驱动程序只能通过内核接口访问进程的内存，且仅在这些进程允许的情况下才可进行访问。这通过*allow*系统调用实现。该系统调用有两种形式：*allow_readonly*和*allow_readwrite*，前者允许驱动程序从该缓冲区读取数据，后者允许驱动程序在该缓冲区中读写数据。

如果进程需要与驱动程序之间发送或接收大量数据，则需要向驱动程序允许一个缓冲区。该进程仍需订阅并等待驱动程序的回调。不这样做，可能会导致从缓冲区读取到错误或不完整的数据，因为进程无法控制驱动程序何时向缓冲区读写数据。

应用程序在访问缓冲区之前必须先取消对其的允许。这意味着要么允许另一个缓冲区，要么允许一个NULL缓冲区。

| 告警 | Tock提供了一个用户空间库，其中定义了针对其驱动程序的API。这意味着编写Tock程序通常不需要直接调用系统调用，而是使用库中提供的API函数。此外，不建议直接调用系统调用，因为驱动程序API可能会随时间变化。不过，在编写新驱动程序时，了解这一模式还是很有帮助的。|
| ----- | ----- |

对于传感器和I/O等若干功能，Tock提供了具有标准API的通用驱动程序。例如，有用于温度、湿度、运动等的驱动程序。当向Tock内核添加新的硬件传感器时，无需编写新的温度驱动程序，只需实现温度硬件接口层（HIL），并将新驱动程序连接到通用温度驱动程序即可。本书后续章节将对此进行详细讨论。

### 进程内存

必须特别注意Tock进程内部的内存组织方式。如果您来自Linux环境，这里的情况会略有不同。图3-4描述了进程的内存布局。由于Tock专为在MCU上运行而设计，因此进程内存被分为两部分：一部分是位于MCU闪存中的只读区域，另一部分是位于RAM中的读写区域。

实际的可执行代码，总所周知在Linux中也称为*text*段，位于闪存中，且为只读（这意味着它是真正的只读，而不仅仅是标记为只读）。这是因为闪存无法直接写入。闪存中还包含*rodata*段，其中存储了常量。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-4.jpg></img></div>
<div align=center>图 3-4 Tock进程内存布局（源：https://github.com/tock/tock）</div>

进程内存中的RAM部分包含*data*段、*stack*段和*heap*段。此外，它还有一个进程无法访问的额外区域，*grant*。首先值得注意的是*stack*的放置位置。它并非位于内存顶部，而是位于底部。做出这一设计决策是为了使能栈溢出检测。如果进程试图在其内存区域之外写入数据，就会引发错误。由于栈位于底部，溢出将导致在进程内存外写数据，从而触发错误。这种方法的缺点是，所有进程都必须在编译时指定其使用的最大栈大小，并且必须严格遵守该限制。此外，栈内存无法与其他段共享。

在栈上方，是*data*段。所有全局变量都存储在此处。进程会在调用main函数之前手动初始化该段。数据段的大小由编译器在编译时自动计算得出。

*data*段的顶部是*heap*。所有动态分配的变量（malloc）都保存在这里。当启动进程时，Tock需要知道需要为其分配多少内存。

| 注意 | 栈通常位于进程内存的末尾，并向下扩展。这种设计旨在使栈在采用虚拟内存的系统（如Linux）中能够根据需要扩展。|
| ----- | ----- |

#### 配额区域

Tock内核实现中的一种全新方法是“配额区域”（grant region）。该区域存储在进程内存的顶部，且进程无法访问。Tock内核利用该区域存储进程控制块（PCB），并为驱动程序分配空间以存储特定于进程的数据。要理解该区域为何至关重要，我们需要深入探讨Tock内核的工作原理。

出于可靠性考虑，Tock内核不允许动态分配内存。这意味着内核使用的所有内存，在编译时需要大致已知。这里使用“大致”一词，是因为驱动程序确实有办法分配一些动态内存，但不像使用Rust语言特性那样直接。

| 告警 | 动态内存分配可能很危险。试想一个驱动程序，每当应用程序向其发出请求时，它都会分配内存。如果该驱动程序因某些错误而未能释放该内存，内核内存最终会溢出，从而导致未定义的行为。由于微控制器的内存非常有限，很容易达到内存上限。|
| ----- | ----- |

无法动态分配内存可能会给某些驱动程序带来问题。许多驱动程序需要为每个进程存储信息，例如计数器或共享缓冲区。根据驱动程序的不同，这些存储信息的大小可能各异，且内核无法预测。像Linux等标准操作系统中，正是出于这些原因，才允许驱动程序动态分配内存。

Tock采取了不同的方法。每个驱动程序都必须定义一个Rust结构体，其中包含其希望为每个进程存储的数据。这被称为“配额”（*grant*）。由于不存在动态驱动程序加载，内核在编译时便能确切知道多少个驱动程序需要配额，以及每个配额的大小。当进程启动时，内核会在新进程的配额区域中为每个驱动程序的配额分配一个 PCB和一个指针。

当驱动程序首次尝试访问进程的配额时，内核会尝试在该驱动程序的配额内为其分配内存空间。如果成功，这意味着该进程有效且配额区域有足够的未使用内存，驱动程序即可存储数据。如果分配失败，驱动程序将无法访问配额，并将向进程报告错误。

这种方法确保了驱动程序要么拥有该进程所需的所有可用配额内存，要么没有进程配额可用。图3-5详细展示了配额（*grant*）区域。使用配额区域的另一个显著优势在于：当进程停止时，其内存将由内核清理。由于驱动程序为该进程分配的所有内存均位于该进程的配额区域内，因此不存在驱动程序分配的内存泄漏风险。换言之，驱动程序只能在进程的配额区域内分配内存，而该区域会在进程停止时被清理。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-5.jpg></img></div>
<div align=center>图 3-5 用于存储指向驱动程序配额，任务队列以及进程控制块的指针列表的配额内存空间</div>

##### 动态配额

我们之前提到，驱动程序无法动态分配内存。但这只说对了一半。有些驱动程序确实需要分配一些在编译时无法预知的额外内存。Tock为此提供了一个名为*DynamicGrant*的结构。它的运作方式与常规的*grant*相同，但允许驱动程序在进程的*grant*区域内请求一些额外空间。

| 注意 | 截至本文撰写之时，Tock中的所有胶囊均未使用*DynamicGrant*。|
| ----- | ----- |

每当驱动程序需要一些额外内存时，它首先必须进入其配额区。一旦该配额被分配并可用，进程即可向内核请求额外空间。内核将尝试在该进程的配额内部分配此额外空间。如果成功，它会向驱动程序返回一个*DynamicGrant*。驱动程序负责将其存储在某个位置。

*DynamicGrant*的访问方式与普通*grant*相同。内核将确保驱动程序无法访问已失效进程中分配的*DynamicGrant*。我们将在本书中详细讨论这一点。

| 注意 | 通常，*DynamicGrants*会存储在普通*grant*中，因为它与某个进程紧密相关。|
| ----- | ----- |

### Tock应用包

Tock操作系统的一个关键区别在于，它能够将应用程序与内核分开构建和部署。这与Linux及其他操作系统发布应用程序的方式非常相似。在深入探讨之前，我们需要定义两个术语：应用程序可执行文件和应用程序包。前者通常指包含二进制可执行代码及部分初始数据的文件，而后者则包含可执行文件以及运行应用程序所需的其他文件，其中包括镜像、视频、声音等数据文件。

对于大多数操作系统而言，可执行文件格式相当标准化，例如Windows的PE、Linux的ELF以及macOS的MachO，但包文件格式却差异巨大。Windows主要使用MSI包；Linux，根据发行版不同，使用DEB、RPM、snap等多种格式；macOS使用DMG或PKG包。移动系统如iOS使用IPA包，而Android则使用APK包。

大多数嵌入式操作系统提供一个单一的二进制文件，以Intel HEX或UF2文件存储，其中内核和所有应用程序紧密地链接在一起。Tock则采取了不同的方法，为内核提供一个二进制文件，而应用程序则使用单独的特定文件。

#### Tock二进制格式

虽然Tock内核是一个普通的二进制文件，但应用程序却大不相同。Tock定义了一种名为“Tock二进制格式”（Tock Binary Format）的可执行文件格式，用于存储应用程序。该文件包含可执行代码、初始数据以及若干描述应用程序加载方式的头文件。图3-6 (a)展示了该格式。文件以TBF头开始，其中包含有关应用程序加载方式的信息。头之后是二进制可执行代码。由于文件必须对齐到4字节边界并满足MPU的限制，因此文件末尾带有填充。

| 提示 | 对于Cortex-M设备，由于MPU区域的大小限制，对齐方式存在若干限制。其中一项限制是，MPU区域的大小必须是2的幂。|
| ----- | ----- |

由于应用程序与内核是分开编译的，因此内核无法得知应用程序的大小、所需内存量或其名称。这个问题通过在二进制文件的TBF头中添加几个标头来解决。图3-6 (b) 展示了TBF头部的格式。它以固定的TBF头部基地址开头（详见图3-6 (c)），其中包含头部格式和大小信息。TBF头部基地址之后是若干个类型-长度-值 (TLV) 元素。TLV元素的结构如图3-6 (d)所示。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-6.jpg></img></div>
<div align=center>图 3-6 Tock二进制格式与TBF头部格式</div>

每个TLV元素都包含类型、长度和数据负载。类型定义了内核应如何解释数据负载中的信息，而长度则表示数据负载的大小。表3-5描述了可用的类型。

Tock应用程序分为两类：可重定位型和固定地址型。前者是真正的Tock标准应用程序，可加载到内存和闪存中的任意地址。它们采用特定方式构建，确保应用程序无论加载在何处都能正常运行。第二类主要是作为备用方案。由于某些编译器问题，部分应用程序无法按上述方式构建，必须在编译时就确定加载内存和地址。目前，Rust应用程序因LLVM问题，以及RISC-V因编译器缺乏适当的重定位功能，均属于静态地址应用程序。

表 3-5 TLV元素类型

| 类型 | 类型名 | 描述 |
| ----- | ----- | ----- |
| 1 | 主(Main) | 提供有关应用程序内存布局的信息。此TLV是必需的，因为内核无法通过其他方式获知所需的内存布局<br>因此必须且仅有一个主元素 |
| 2 | 可写闪存区域(Writable flash region) | 定义应用程序应被允许写入的MCU闪存区域。该区域用于在MCU重启之间存储持久性数据<br>可能存在多个可写闪存区域元素，每个请求的区域对应一个元素 |
| 3 | 包名(Package name) | 存储应用程序的名称<br>必须且仅有一个*package name*元素 |
| 4 | 固定地址(Fixed address) | 只有当应用程序不可重定位且必须加载到固定的内存和闪存位置时，才会出现这种情况。<br>固定地址元素最多只能有一个。<br>否则，内核将尝试重定位该应用程序。|
| 5 | 权限(Permissions) | 指定应用程序被允许调用的胶囊编号及其命令编号的集合。Tock 2.0目前尚未实现这些限制。|
| 6 | 持久的ALCL(Persistent ACL) | 提供存储空间的访问规则。Tock 2.0 目前尚未根据该标头中的信息实施任何限制。|
| 7 | 内核版本(Kernel version) | 确保应用程序与当前运行的内核版本兼容。Tock在内核与应用程序之间使用应用程序二进制接口（ABI）。由于内核和应用程序是分别编译的，内核必须确保应用程序是使用相同的ABI编译的；否则，应用程序可能无法正常运行。|

我们将在关于应用程序开发的章节中详细讨论所有这些要素。

| 注意 | 表3-5中列出的类型在撰写本书时是有效的。由于本书出版后可能会有新的类型被定义，建议您查阅Tock官方文档。|
| ----- | ----- |

#### Tock应用程序捆绑

由于Tock运行在多种不同的MCU架构上，应用程序的二进制代码会因平台而异。Tock允许开发者构建应用程序包，并将其分发到多个平台。这通过一种名为Tock应用程序包（TAB）的文件格式来实现。

该文件格式是一个tar归档文件，其中包含多个TBF文件，每个支持的平台对应一个，以及描述每个TBF文件的清单。TAB文件由tockloader解析，这是一个用于在设备上部署Tock的命令行工具。在对设备进行编程时，tockloader会读取TAB文件，选择对应平台的TBF文件，并将其上传。

TAB文件中的二进制文件必须采用以下格式命名：<architecture>.<anything>.tbf。文件名中的*anything*部分可以是任何字符串值。

## 刷写系统

嵌入式系统开发的一个关键步骤是将软件写入设备。这个过程通常被称为“刷写”或“编程”。对于Tock而言，这意味着将内核的二进制代码和应用程序的TBF文件加载到MCU的闪存中。虽然在大多数嵌入式系统中，“刷写”和“编程”这两个术语通常可以互换使用，但在Tock中，术语的使用却明确区分了二者。在Tock的术语体系中，“刷写”指通过JTAG接口加载代码，而“编程”则指通过串行连接将代码加载到引导加载程序中。

目前，我们将使用“上传”一词来指代将软件组件写入MCU闪存的过程。图3-7展示了软件上传的两种方式：刷写或编程。

<div align=center><img src=Figures/Chapter-3-Screenshot/Figure-3-7.jpg></img></div>
<div align=center>图 3-7 刷写(a)与编程(b)Tock内核与应用程序</div>

### 调试芯片

在编写计算机软件时，操作系统会提供一些基础功能，使调试器软件，例如gdb，能够在特定位置暂停程序、读取内存、显示寄存器值等。这非常重要，因为查找程序中的错误并非易事。使用调试器软件的另一方案是在屏幕上输出大量信息。

至于微控制器，调试则变得更加困难。首先，与计算机软件（其代码在同一台机器上运行）不同，微控制器代码运行在与您正在操作的硬件不同的硬件上。其次，微控制器通常不具备屏幕，无法在屏幕上打印调试信息。为解决这一问题，微控制器配备了专用的调试接口，允许外部设备在特定点暂停程序、读取内存或寄存器值。调试接口有多种标准，最常见的是JTAG或SWD（ARM专用）。

尽管所有微控制器都配备了调试接口，但要使用它，必须将其连接到硬件调试器，例如SEGGER JLink或STM ST-Link。该硬件调试器则需连接到计算机的USB端口。

| 注意 | 某些开发板，如micro:bit v2，内置了嵌入式硬件调试器，可以直接连接到计算机并进行调试。而其他开发板，如Raspberry Pi Pico，则不具备此功能，需要连接外部硬件才能通过计算机进行调试。|
| ----- | ----- |

### 刷写

在Tock系统中，“刷写”指的是利用硬件调试接口烧写Tock软件、内核和应用程序。这需要专用软件，如OpenOCD¹和专用硬件，如JLink或ST-Link。这种方法并非Tock系统所独有，因为任何可执行代码都可以通过此方法上传。图3-7 (a)展示了该方法。

然而，使用刷写有两个优势：
1. 微控制器无需运行任何额外软件，且可充分利用所有可用闪存空间。
2. 系统无法锁定用户，这意味着无论发生什么问题，用户总能重新烧录微控制器。

第二点非常重要。在将软件写入闪存时，可能会出现问题。例如，可能会发生断电，导致闪存只写入了一半。这将导致微控制器无法正常工作。而使用硬件调试器时，则不会有这个问题。你只需重新尝试即可。

### 编程



【注1】OpenOCD (Open On-Chip Debugger) 这是一个开源的片上调试器，将类似gdb的软件调试器与设备上的硬件调试器连接起来。