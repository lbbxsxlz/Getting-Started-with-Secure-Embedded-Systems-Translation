# 用于Tock的Rust

我们已经到了开始为Tock内核编写代码的阶段。由于Tock是一个用Rust编写的操作系统，因此这需要一些基本的Rust编程知识。因此，本章将重点介绍Rust编程语言所具备的一些重要特性。在描述Rust的特殊性时，我们假设读者熟悉C/C++等其他编译型编程语言，并且了解指针和内存分配的工作原理。此外，还需具备关于Rust语言构造（如数据类型、结构体、枚举和特质）的基础知识。

本章仅详细介绍Rust基本数据类型、结构体、枚举和特质的必要内容。我们将重点关注Rust的引用和生命周期。

## Rust介绍

首先，我们需要了解一些Rust的基础概念。请注意，这只是一篇非常简短的介绍。我们强烈建议您阅读Rust Book¹中的相关章节。

Rust是一种致力于高度安全同时不牺牲性能的编程语言。虽然像Java或Python这样的编程语言提供了高度的安全性，但是以性能为代价的。Rust通过强制采用严格的编程模式，使编译器能够在编译时做出若干假设，从而提供了同等的安全保障，却无需牺牲性能。换句话说，它既拥有C语言的速度，又具备Java语言的安全性，但学习起来更具挑战性。

在本章的剩余部分，我们将假设读者已经熟悉Rust语言的基本语法，例如变量声明、函数声明以及控制流（如if、for和while）。

接下来，我们将通过与C/C++和Java的类比来介绍Rust。

### Rust基本数据类型

Rust提供了一组有限的被视为基本类型的数据类型。它们表示数字和字符。表4-1描述了这些类型。

表 4-1 Rust基本数据类型和它们的值区间

| 名称 | 描述 | 值 |
| ----- | ----- | ----- |
| () | 单位 | () |
| char | UTF-8格式的单个字符（4字节）| UTF-8 code point |
| bool | 布尔值 | true, false |
| u8 | 无符号单字节整型 | 0 ... 255 |
| u16 | 无符号两字节整型 | 0 ... 65535 |
| u32 | 无符号四字节整型 | 0 ... 4,294,967,295 |
| u64 | 无符号八字节整型 | 0 ... 2<sup>64</sup> - 1 |
| u128 | 无符号十六字节整型 | 0 ... 2<sup>128</sup> - 1 |
| i8 | 有符号单字节整型 | -128 ... 127 |
| i16 | 有符号两字节整型 | -32768 ... 32767 |
| i32 | 有符号四字节整型 | -2，147，483，648 ... 4,294,967,295 |
| i64 | 有符号八字节整型 | -2<sup>63</sup> ... 2<sup>63</sup> - 1 |
| i128 | 有符号十六字节整型 | -2<sup>127</sup> ... 2<sup>127</sup> - 1 |
| usize | 指针大小无符号整型 | 0 ... -2<sup>pointer_size</sup> - 1 |
| isize | 指针大小有符号整型 | -2<sup>pointer_size</sup> ... 2<sup>pointer_size</sup> - 1 |
| f32 | 单精度（4字节）浮点数 | |
| f64 | 双精度（8字节）浮点数 | |

| 注意 | 与C语言不同，Rust与Java类似，明确区分了布尔型和数值型。数值不会自动转换为布尔值，反之亦然。换句话说，0不被视为等于false。|
| ----- | ----- |

必须特别注意字符（char）类型。Rust使用UTF-8字符，而C语言使用ASCII，Java则使用UTF-16。在C语言中，一个字符占用1字节，在Java中占用2字节，但在Rust中，一个字符最多可占用4字节。char数据类型始终为4字节长，以便能够容纳任何使用UTF-8编码的Unicode字符。

| 注意 | UTF-8允许使用可变长度对Unicode字符进行编码。一个字符的长度可能为1字节（如ASCII字符），最多可达4字节。|
| ----- | ----- |

### 可变性

Rust定义了一个在C或Java中不适用的概念：可变性。默认情况下，所有声明的变量都被视为不可变的。这意味着一旦赋值，其值就无法被修改。清单4-1概述了这一概念。变量v是不可变的，无法被重新赋值。另一方面，变量v2被声明为可变的（使用let mut），因此可以被重新赋值。

清单 4-1 Rust可变性例子

```
fn main() {
    let v = 1;
    v = v + 1; // ERROR: v is immutable
    println!("v {}", v);
    let mut v2 = 1;
    v2 = v2 + 1;
    println!("v2 {}", v2);
}
```

Rust会推断变量v1和v2的实际数据类型。Rust编译器可以通过查看赋予这两个变量的值，确定它们的数据类型为u32。如果我们要使用不同的数据类型，就必须声明变量的数据类型，或者为常量添加数据类型。清单4-2展示了对清单4-1的重写，其中v具有声明的类型，而v2是一个带有数据类型的常量。

清单 4-2 Rust数据类型

```
fn main() {
    let v: u8 = 1;
    v = v + 1; // ERROR: v is immutable
    println!("v {}", v);
    let mut v2 = 1u8;
    v2 = v2 + 1;
    println!("v2 {}", v2);
}
```

| 注意 | 对于整数，Rust会自动使用u32数据类型；而对于浮点数，则会使用f64。|
| ----- | ----- |

另一个有趣的方面是，引用数据类型（&Type和&mut Type）包含可变性信息。例如，在let vr = &v 中，vr的类型是&u32；而在let vr2 = &mut v2 中，v2的数据类型是&mut u32。

| 注意 | 尽管常量可以被视为不可变变量，但二者之间存在区别。编译器会将常量放置在独立的内存空间中，而不可变变量则是标准变量，只不过编译器不允许对其重新赋值。常量需要在编译时具有已知的值，而不可变变量则不需要。|
| ----- | ----- |

### 谁拥有值？

Rust引入了“所有权”的概念。对于每个值，只能有一个所有者。例如，如果我们声明一个变量v并为其赋值，那么v就拥有被赋给它的那个值。当我们将另一个变量赋值给某个变量时，就会出现一个有趣的问题。清单4-3描述了这一问题。

清单 4-3 Rust所有者问题

```
fn main() {
    let v: u32 = 1; // v owns the value
    let v2 = v; // who owns the value, v2 or v?
}
```

首先，将值1赋给变量v。这意味着v拥有该值。实际上，它拥有存储在该内存位置上的任何值。那么，当将v的值赋给v2时会发生什么？Rust应用以下规则：
1. 如果能够复制该值，则在赋值前先进行复制；此时v2拥有该值的副本；
2. 如果无法复制该值，则会将其移动。该值的所有者将变为v2而不是v。此后若继续使用v变量，将会引发编译器错误。

接下来的问题是：Rust如何判断能否复制一个值？这便引出了特质（traits）的概念。简而言之，特质描述了一组可应用于数据类型的功能。如果你熟悉Java，特质类似于接口。如果一个类定义了该接口中描述的所有方法，则称该类实现了该接口。与Java不同的是，特质可以应用于任何数据类型。本章后文将深入探讨Rust的trait。

在进行复制或移动操作时，Rust编译器会检查该数据类型是否实现了*Copy*特质。如果实现了，它将复制该值；否则，它将移动该值。表4-1 中列出的所有基本数据类型都实现了*Copy*特质，这意味着清单4-3中描述的示例将对该值进行复制。

清单 4-4 Rust中拷贝值

```
fn main() {
    // v owns the value
    let v: u32 = 1;
    // who owns the value, v2 or v?
    let mut v2 = v;
    v2 = v2 + 1;
    println!("v {}", v);
    println!("v2 {}", v2);
}
```

为了明确说明Rust会执行复制操作，清单4-4进一步扩展了该示例，并修改了v2的值。打印时，v的值将为1，而v2由于被递增，其值将为2。

虽然基本数据类型默认实现了*Copy*，但复杂类型则不然。清单4-5展示了一个定义复杂数据类型的示例，该类型是一个包含数字的结构体。我们将一个结构体（即该值）赋值给变量v，然后将变量v赋值给v2。Rust编译器会检查Number结构体是否实现了*Copy*。由于它未实现，编译器将移动该值。当尝试打印变量v中的数值时，编译器会报错，因为变量v不再拥有该值，因此无法被使用。

清单 4-5 Rust中移动值

```
struct Number {
    n: u32
}

fn main() {
    let v = Number {n:1};
    let v2 = v;
    println!("v.n {}", v.n); // ERROR - v does // not own the value, it wasmoved to v2
    println!("v2.n {}", v2.n);
}
```

### 字符串

能够存储文本是任何编程语言的一项非常重要的特性。Rust提供了两种不同的数据类型来处理字符串：String数据类型和字符串切片（&str）。String数据类型定义在Rust的标准库（std）中，并使用堆分配。另一方面，字符串切片是对一个包含文本且长度固定的数组（[u8]）的内存位置的引用。

#### 堆分配的字符串

与Java类似，Rust提供了一种名为String的特殊类型，用于存储一串字符。在内部，Rust存储了三个值：对缓冲区的引用、所存储字符串的长度（以UTF-8码点为单位）以及容量（以字节为单位）。清单4-6展示了（经验上）String的定义方式。&符号表示数组切片，我们稍后将对此进行讨论。目前，只需将其视为指向一个字节数组的引用（与C语言中的char[]类型相同）。

清单 4-6 Rust字符串类型的经验上定义

```
struct String {
    buffer: &[u8],
    len: usize,
}
```

| 注意 | C语言实际上并未提供字符串类型，而是使用普通的字符数组（char[]），并通过一种特殊约定来标记字符串的结尾。这是C语言中最危险的方面之一，也极有可能是攻击的主要途径。|
| ----- | ----- |

接下来，让我们仔细看看String结构的各个元素。buffer属性是实际的存储空间。这在某种程度上类似于C语言中的字符串，因为它是一个简单的字符数组，用于存储字节。String的有效长度由len属性存储。这意味着String包含多少个有效字符。请记住，每个字符都是一个UTF-8码点，因此一个字符可能占用1到4个字节。

那么，第三个值又是什么呢？我们之前提到String存储三个值，但该结构体只定义了两个。第三个值，即String的容量，是数组的大小（以字节为单位）。与C语言的数组不同，Rust中的数组和数组切片（引用）由两个值描述：它们所包含的实际数据及其长度。通过使用buffer.len()，我们可以获取数组的长度。

Rust字符串与C或Java字符串之间的一个关键区别在于访问单个字符的方式。C和Java都采用s[position]这种数组访问方式。这种方式在C中可行，因为字符串就是数组；而在Java中，字符串存储的是Unicode字符，每个字符恰好占用2字节，因此同样适用。由于Rust存储的是UTF-8编码，字符长度是可变的。访问字符串缓冲区中的某个随机元素时，该位置可能不在字符边界上，而可能位于某个UTF-8码点的中间。这就是为什么Rust字符串提供了一个能够遍历所有字符的迭代器。通过使用标准的迭代器函数，我们可以获取特定的字符。清单4-7展示了一个这样的示例。

清单 4-7 创建一个新的字符串并访问一个字符

```
fn main() {
    let s = String::from("Hello from Rust, 😄 how are you?");
    println!("{}", s[10]);
    // ERROR - does not implement the Index trait
    println!("{}", s.chars(). nth(10).unwrap());
    // uses an iterator
    println!("{}", s);
}
```

| 注意 | 使用from方法可以创建新的字符串。只是简单地将s赋值为“...”，则不会生成一个新的字符串，而是生成一个字符串切片（&str）。|
| ----- | ----- |

一个重要特征是，String类并未实现*Copy*，这意味着在进行赋值时，字符串会采用移动方式。做出这一决定的原因是出于性能考虑，因为复制字符串需要花费一定时间来复制缓冲区。清单4-8对此进行了说明。

清单 4-8 字符串赋值是移动操作

```
fn main() {
    let title = String::from("The Title");
    let the_title = title;
    // ERROR
    // title does not own the value anymore
    println!("title {}", title);
    println!("the_title {}", the_title);
}
```

在某些情况下，我们可能希望复制字符串，而不是移动它。为此，String实现了Clone特性，该特性提供了clone方法，可用于执行复制操作（清单4-9）。

清单 4-9 Rust中移动字符串

```
fn main() {
    let title = String::from("The Title");
    let the_title = title.clone();
    println!("title {}", title);
    println!("the_title {}", the_title);
}
```

#### 字符串切片

切片是一种特殊的引用类型，主要用于数组和字符串变量。对于字符串而言，切片是对字符串内部缓冲区的引用。与字符串引用不同，切片仅存储长度参数，其大小无法更改。

字符串切片的类型是&str。在大多数情况下，函数会将此类型作为参数。Rust将字符串类型定义为&str。这就是为什么我们在清单4-10中使用let a = String::from(“...”)而不是let a = “...”来定义字符串。

| 注意 | Rust 会自动将字符串引用（&String）强制转换为字符串切片（&str）。|
| ----- | ----- |

清单 4-10 字符串与字符串切片之间的区别

```
fn main() {
    let string = String::from("The Title");
    let string_slice = "The Title";
    // use these to display the types
    // let _: () = string;
    // let _: () = string_slice;
}
```

Tock不使用String数据类型，因为该数据类型属于标准库（std），而Tock并未使用该标准库。除非包含标准库，否则无法使用 String数据类型，因为它使用堆分配。这需要一个堆分配器，而该分配器在标准库中提供。另一方面，Tock确实使用在数据段中分配的字符串切片（&str）（即常量字符串 “.....”）。

| 注意 | 字符串引用（&String）与字符串切片（&str）之间的区别很微妙。由于&String是对String的引用，任何以可变方式借用该字符串的人都可以对其进行任意修改。而使用字符串切片（&str）时，即使它是可变的（&mut str），其长度也不能被更改。|
| ----- | ----- |

### 借用

既然我们已经理解了值的所有权，接下来就必须讨论值的另一个重要用法：将其作为参数传递给函数。让我们以清单4-11中的示例为例，该示例描述了一个将两个数字相加的函数。随之而来的问题是，a和b的值的所有权会发生什么变化。Rust编译器对函数参数适用的规则与对赋值的规则相同。如果数据类型实现了*Copy*特质，则会复制值；否则，将移动值。

清单 4-11 一个计算两个数字之后的函数

```
fn sum(a: u32, b: u32) -> u32 {
    a + b
}

fn main() {
    let a = 1;
    let b = 2;
    let s = sum(a, b);
    println!("sum {}", s);
    // works as the value has been copied to sum
    println!("a {}", a);
}
```

在此示例中，一切运行正常，因为u32是基本类型，而基本类型实现了*Copy*特性。现在，让我们看看当使用String代替数字时会发生什么。请记住，String并未实现*Copy*特性。清单4-12声明了一个用于求两个String变量之和的函数。当调用string_sum时，参数的值会被移入函数，且不再可用。这意味着变量a和b无法再被使用。

| 注意 | format!宏的工作原理与println!宏相似，唯一的区别在于它会返回一个包含该文本的新分配的String对象，而不是直接打印格式化的文本。|
| ----- | ----- |

清单 4-12 字符串参数移动到函数

```
fn string_sum(a: String, b: String) -> String {
    format!("{}{}",a, b)
}

fn main() {
    let a = String::from("The");
    let b = String::from(" Title");
    let s = string_sum(a, b);
    println!("sum {}", s);
    // ERROR
    // the value has been moved to string_sum
    println!("a {}", a);
}
```

问题在于，我们该如何解决这个问题？一个想法是在传递参数之前先克隆它们，但这并不高效。此外，函数在利用参数内容创建新的字符串时，仅需临时使用这些参数。当函数执行完毕后，这两个变量应能再次被使用。

这就是“借用”概念发挥作用的地方。Rust允许我们在函数需要这些值时将其借用给函数。这意味着我们仍然拥有这些值的所有权。只是在借用期间，对它们的使用可能会受到一些限制。

| 注意 | 虽然其他语言（如C或Java）采用“按引用传递值”的方式，但Rust更进一步，提出了“借用”的概念。换句话说，对一个值的借用既是对该值的引用，同时也包含一种遵循特定规则的可变性标注。在任何给定时刻，只能存在一个可变引用，或者任意多个不可变引用。|
| ----- | ----- |

在借用方面，Rust有两条基本规则。在某个时刻，系统中可以存在：

• 任意数量的不可变借用；

• 且仅有一个可变借用。

只要遵循这些规则，Rust就能在编译时保证内存安全，而无需依赖运行时库或垃圾回收机制。

要使用借用，我们只需修改函数参数，使其接收指向String的引用。清单4-13展示了修改后的函数。

清单 4-13 借用字符串

```
fn string_sum(a: &String, b: &String) -> String {
    format!("{}{}", a, b)
}

fn main() {
    let a = String::from("The");
    let b = String::from(" Title");
    // immutable borrow a and b
    let s = string_sum(&a, &b);
    println!("sum {}", s);
    // works as a still owns the value
    println!("a {}", a);
}
```

这看起来可能与其他语言没什么不同，但关于借用、引用以及Rust如何提供安全保证，还有更多值得探讨的内容。我们将在“生命周期”一节中讨论这一点。

