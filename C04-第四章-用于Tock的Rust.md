# 用于Tock的Rust

我们已经到了开始为Tock内核编写代码的阶段。由于Tock是一个用Rust编写的操作系统，因此这需要一些基本的Rust编程知识。因此，本章将重点介绍Rust编程语言所具备的一些重要特性。在描述Rust的特殊性时，我们假设读者熟悉C/C++等其他编译型编程语言，并且了解指针和内存分配的工作原理。此外，还需具备关于Rust语言构造（如数据类型、结构体、枚举和特质）的基础知识。

本章仅详细介绍Rust基本数据类型、结构体、枚举和特质的必要内容。我们将重点关注Rust的引用和生命周期。

## Rust介绍

首先，我们需要了解一些Rust的基础概念。请注意，这只是一篇非常简短的介绍。我们强烈建议您阅读Rust Book¹中的相关章节。

Rust是一种致力于高度安全同时不牺牲性能的编程语言。虽然像Java或Python这样的编程语言提供了高度的安全性，但是以性能为代价的。Rust通过强制采用严格的编程模式，使编译器能够在编译时做出若干假设，从而提供了同等的安全保障，却无需牺牲性能。换句话说，它既拥有C语言的速度，又具备Java语言的安全性，但学习起来更具挑战性。

在本章的剩余部分，我们将假设读者已经熟悉Rust语言的基本语法，例如变量声明、函数声明以及控制流（如if、for和while）。

接下来，我们将通过与C/C++和Java的类比来介绍Rust。

### Rust基本数据类型

Rust提供了一组有限的被视为基本类型的数据类型。它们表示数字和字符。表4-1描述了这些类型。

表 4-1 Rust基本数据类型和它们的值区间

| 名称 | 描述 | 值 |
| ----- | ----- | ----- |
| () | 单位 | () |
| char | UTF-8格式的单个字符（4字节）| UTF-8 code point |
| bool | 布尔值 | true, false |
| u8 | 无符号单字节整型 | 0 ... 255 |
| u16 | 无符号两字节整型 | 0 ... 65535 |
| u32 | 无符号四字节整型 | 0 ... 4,294,967,295 |
| u64 | 无符号八字节整型 | 0 ... 2<sup>64</sup> - 1 |
| u128 | 无符号十六字节整型 | 0 ... 2<sup>128</sup> - 1 |
| i8 | 有符号单字节整型 | -128 ... 127 |
| i16 | 有符号两字节整型 | -32768 ... 32767 |
| i32 | 有符号四字节整型 | -2，147，483，648 ... 4,294,967,295 |
| i64 | 有符号八字节整型 | -2<sup>63</sup> ... 2<sup>63</sup> - 1 |
| i128 | 有符号十六字节整型 | -2<sup>127</sup> ... 2<sup>127</sup> - 1 |
| usize | 指针大小无符号整型 | 0 ... -2<sup>pointer_size</sup> - 1 |
| isize | 指针大小有符号整型 | -2<sup>pointer_size</sup> ... 2<sup>pointer_size</sup> - 1 |
| f32 | 单精度（4字节）浮点数 | |
| f64 | 双精度（8字节）浮点数 | |

| 注意 | 与C语言不同，Rust与Java类似，明确区分了布尔型和数值型。数值不会自动转换为布尔值，反之亦然。换句话说，0不被视为等于false。|
| ----- | ----- |

必须特别注意字符（char）类型。Rust使用UTF-8字符，而C语言使用ASCII，Java则使用UTF-16。在C语言中，一个字符占用1字节，在Java中占用2字节，但在Rust中，一个字符最多可占用4字节。char数据类型始终为4字节长，以便能够容纳任何使用UTF-8编码的Unicode字符。

| 注意 | UTF-8允许使用可变长度对Unicode字符进行编码。一个字符的长度可能为1字节（如ASCII字符），最多可达4字节。|
| ----- | ----- |

### 可变性

Rust定义了一个在C或Java中不适用的概念：可变性。默认情况下，所有声明的变量都被视为不可变的。这意味着一旦赋值，其值就无法被修改。清单4-1概述了这一概念。变量v是不可变的，无法被重新赋值。另一方面，变量v2被声明为可变的（使用let mut），因此可以被重新赋值。

清单 4-1 Rust可变性例子

```
fn main() {
    let v = 1;
    v = v + 1; // ERROR: v is immutable
    println!("v {}", v);
    let mut v2 = 1;
    v2 = v2 + 1;
    println!("v2 {}", v2);
}
```

Rust会推断变量v1和v2的实际数据类型。Rust编译器可以通过查看赋予这两个变量的值，确定它们的数据类型为u32。如果我们要使用不同的数据类型，就必须声明变量的数据类型，或者为常量添加数据类型。清单4-2展示了对清单4-1的重写，其中v具有声明的类型，而v2是一个带有数据类型的常量。

清单 4-2 Rust数据类型

```
fn main() {
    let v: u8 = 1;
    v = v + 1; // ERROR: v is immutable
    println!("v {}", v);
    let mut v2 = 1u8;
    v2 = v2 + 1;
    println!("v2 {}", v2);
}
```

| 注意 | 对于整数，Rust会自动使用u32数据类型；而对于浮点数，则会使用f64。|
| ----- | ----- |

另一个有趣的方面是，引用数据类型（&Type和&mut Type）包含可变性信息。例如，在let vr = &v 中，vr的类型是&u32；而在let vr2 = &mut v2 中，v2的数据类型是&mut u32。

| 注意 | 尽管常量可以被视为不可变变量，但二者之间存在区别。编译器会将常量放置在独立的内存空间中，而不可变变量则是标准变量，只不过编译器不允许对其重新赋值。常量需要在编译时具有已知的值，而不可变变量则不需要。|
| ----- | ----- |

### 谁拥有值？

